import React from 'react';
import { Outlet } from "react-router-dom";



const Parametres = () => {
  
    return (
        <div >
          <Outlet />
        </div>
      );
};

export default Parametres;
